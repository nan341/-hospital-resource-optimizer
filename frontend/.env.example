# Frontend Environment Configuration for LAN / Cross-Device Access
# Replace <SERVER_IP> with your backend server machine's local IPv4 address (e.g., 192.168.1.50)

VITE_API_BASE=http://<SERVER_IP>:8000
VITE_WS_BASE=ws://<SERVER_IP>:8000
